//Interface //in HTML verlinken!!!
namespace transformations {
    export interface Snowflake { //Export: darf außerhalb der Datei (aber im selben Namespace verwendet werden)
        x: number;
        y: number;
        radius: number;
    }
}